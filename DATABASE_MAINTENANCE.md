# Обслуживание базы данных моделей AI

## 🎯 Обзор
Этот документ описывает процесс обслуживания и обновления базы данных моделей AI для проекта cursor-benchmark.

## 📊 Текущая статистика базы
- **Моделей всего:** 87 (уникальных, без дубликатов)
- **Заполненность данных:**
  - Контекст: 83.9% (73/87 моделей)
  - Цены: 82.8% (72/87 моделей)
  - Доступность в Cursor: 100% (87/87 моделей)
  - Возможности: 100% (87/87 моделей)
- **Источников бенчмарков:** 31
- **Результатов бенчмарков:** 1,228

## 🔧 Доступные скрипты

### Обновление данных из интернета
```bash
# Улучшенный парсер с умным сопоставлением моделей
npm run scrape:enhanced -- --all

# Отдельные этапы
npm run scrape:enhanced -- --scrape  # Спарсить данные
npm run scrape:enhanced -- --update  # Обновить базу
```

### Очистка дубликатов
```bash
# Анализ дубликатов
npm run clean:duplicates -- --analyze

# Очистка дубликатов (ОСТОРОЖНО!)
npm run clean:duplicates -- --clean
```

### Экспорт данных
```bash
# Экспорт полных данных для статического режима
npm run export:full

# Экспорт только основных данных
npm run export:static
```

## 🚀 Полный процесс обновления

### 1. Скачивание свежих данных
```bash
npm run scrape:enhanced -- --scrape
```

### 2. Анализ дубликатов
```bash
npm run clean:duplicates -- --analyze
```

### 3. Очистка дубликатов (если найдены)
```bash
npm run clean:duplicates -- --clean
```

### 4. Обновление базы данных
```bash
npm run scrape:enhanced -- --update
```

### 5. Экспорт для GitHub Pages
```bash
npm run export:full
```

### Команда "всё в одном"
```bash
npm run scrape:enhanced -- --all && npm run clean:duplicates -- --clean && npm run export:full
```

## 🎨 Особенности улучшенного парсера

### Умное сопоставление моделей
- **Нормализация названий:** Убирает префиксы, версии, специальные символы
- **Многоуровневое сравнение:**
  1. Точное совпадение displayName
  2. Совпадение modelId
  3. Нормализованное совпадение
  4. Частичное совпадение с оценкой уверенности

### Обработка данных
- **Контекст:** Берет максимальное значение из доступных
- **Цены:** Конвертирует $/M токенов в $/токен
- **Возможности:** Объединяет уникальные capabilities
- **Cursor доступность:** Отмечает все спарсенные модели

### Очистка дубликатов
- **Выбор главной модели:** По количеству данных (контекст, цены, оценки, бенчмарки)
- **Объединение данных:** Лучшие значения из всех дубликатов
- **Перенос связанных данных:** Оценки и бенчмарки перемещаются к главной модели

## 📋 Провайдеры моделей

| Провайдер | Количество | Примеры |
|-----------|------------|---------|
| OpenAI | 20 | GPT-4, GPT-5, GPT-4o, etc. |
| Anthropic | 14 | Claude 3.5 Sonnet, Opus, Haiku |
| xAI | 8 | Grok 2, Grok-1.5, etc. |
| Meta | 7 | Llama 3.1 405B, 70B, 8B |
| DeepSeek | 6 | DeepSeek Coder, Chat, R1 |
| Google | 5 | Gemini 1.5 Pro, Flash |
| Mistral | 5 | Codestral, Large, Medium |
| Cohere | 2 | Command R, R+ |
| Cursor | 2 | Composer 1, Small |
| Остальные | 18 | Разные провайдеры |

## 🔍 Источники бенчмарков

31 источник covering:
- **Coding:** aider, humaneval, mbpp, codeforces, swe_bench
- **Knowledge:** mmlu, gpqa, agieval
- **Reasoning:** arc, hellaswag, bbh
- **Math:** gsm8k, math
- **Multilingual:** xquad, xnli
- **Chat:** mt_bench, chatbot_arena
- **Safety:** jailbreak, truthfulqa
- **Comprehensive:** open_llm_leaderboard, helm

## ⚡ Быстрые команды

```bash
# Полная синхронизация
npm run scrape:enhanced -- --all

# Проверка на дубликаты
npm run clean:duplicates -- --analyze

# Финальный экспорт
npm run export:full
```

## 📝 Примечания

- Все скрипты работают с SQLite базой данных через Prisma
- Данные экспортируются в `static-models-data.json` для статического режима
- Очистка дубликатов необратима - делайте бэкап перед запуском
- Улучшенный парсер устойчив к изменениям структуры сайта Cursor
